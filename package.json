{
  "name": "neo-rewards-demo-app",
  "private": true,
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": ">=22.10.0"
  },
  "scripts": {
    "lint": "echo \"linting\"",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "workspaces": [
    "packages/*",
    "client"
  ]
}
